[project]
name = "feed-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "motor",
    "redis[hiredis]",
    "elasticsearch[async]",
    "pydantic-settings",
]
