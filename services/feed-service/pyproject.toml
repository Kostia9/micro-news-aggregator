[project]
name = "feed-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "aiokafka",
    "motor",
    "pydantic-settings",
]

[tool.pytest.ini_options]
addopts = "--capture=no"
testpaths = ["tests"]
pythonpath = ["."]
